version: '3.8'

services:
  app:
    container_name: studee-ui
    build: .
    ports:
      - "3000:3000"
    restart: always
    working_dir: /app
    volumes:
      - ./:/app
    command: sh -c "npm install && npm run dev"

  # nginx:
  #   image: nginx:alpine
  #   container_name: nginx-proxy
  #   ports:
  #     - "80:80"
  #     - "443:443"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - /etc/letsencrypt:/etc/letsencrypt:ro    
  #   depends_on:
  #     - app
  #   restart: always